# This file makes 'adapters' a Python package.
# We keep it empty of imports to prevent circular dependency errors.
#
# Usage:
# from pipeline.adapters import refm_adapt
# from pipeline.adapters import pmd_adapt